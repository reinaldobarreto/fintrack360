<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Corpo da carteira (foreground branco para contrastar com fundo) -->
    <item android:left="18dp" android:right="18dp" android:top="30dp" android:bottom="30dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>

    <!-- Aba/fecho da carteira -->
    <item android:left="54dp" android:right="26dp" android:top="44dp" android:bottom="44dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>

    <!-- Botão circular pequeno do fecho -->
    <item android:left="64dp" android:top="58dp" android:bottom="58dp" android:right="64dp">
        <shape android:shape="oval">
            <solid android:color="#FFFFFF"/>
        </shape>
    </item>
</layer-list>